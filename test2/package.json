{
  "name": "test2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test",
    "clean": "lerna clean",
    "test:unit": "lerna run test:unit --scope={@project/riot}",
    "serve": "lerna run serve --scope={@project/riot}",
    "build:local": "lerna run build:local --scope={@project/riot}",
    "start": "lerna run start --scope={@project/riot}"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
